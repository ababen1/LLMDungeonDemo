import { COLORS } from '../render/colors.js';

export const DEFAULT_PARAMS = {
  seed: 42,
  difficulty: 3,
  density: 3,
};

export const GLOBAL_TIMEOUT_MS = 10000;
export const GENERATION_BUDGET_MS = 6500;
export const REPAIR_BUDGET_MS = 3000;

export { COLORS };
